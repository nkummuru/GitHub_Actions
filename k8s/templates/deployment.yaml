apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-app
  labels:
    app: python-app
spec:
  replicas: 2                           # Number of Pods (scalable)
  selector:
    matchLabels:
      app: python-app
  template:
    metadata:
      labels:
        app: python-app
    spec:
      containers:
      - name: python-container
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}" # Image from your container registry
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - containerPort: 8080                        # Flask app runs on port 8080
        env:
        - name: FLASK_ENV
          value: "dev"                       # Example environment variable
