image:
  repository: ghcr.io/nkummuru/github_actions
  tag: 20250502v1
  pullPolicy: IfNotPresent

replicaCount: 2
service:
  type: NodePort        # Service type
  port: 80              # Internal cluster port
  targetPort: 8080      # Container port
  nodePort: 30008       # Node port exposed externally
